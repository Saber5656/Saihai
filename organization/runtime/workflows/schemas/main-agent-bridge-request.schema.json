{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sahai.local/schemas/workflows/main-agent-bridge-request.schema.json",
  "title": "Main Agent Bridge Request",
  "description": "Restricted submit_request payload accepted from the main-agent confirmation surface.",
  "type": "object",
  "required": [
    "task_id",
    "request_id",
    "request_kind",
    "prompt",
    "refs",
    "idempotency_key"
  ],
  "properties": {
    "task_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]{0,95}$"
    },
    "request_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]{0,95}$"
    },
    "request_kind": {
      "type": "string",
      "enum": [
        "external_review_request",
        "orchestrator_status_request"
      ]
    },
    "prompt": {
      "type": "string"
    },
    "refs": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "allowed_paths": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "expires_at": {
      "type": "string"
    },
    "frontdoor": {
      "type": "string",
      "enum": [
        "codex",
        "claude",
        "manual"
      ]
    },
    "chat_session_id": {
      "type": "string"
    },
    "idempotency_key": {
      "type": "string",
      "minLength": 1
    }
  },
  "not": {
    "anyOf": [
      {
        "required": [
          "classification"
        ]
      },
      {
        "required": [
          "activation"
        ]
      },
      {
        "required": [
          "workflow_selection"
        ]
      },
      {
        "required": [
          "run_id"
        ]
      },
      {
        "required": [
          "report_path"
        ]
      },
      {
        "required": [
          "principal_type"
        ]
      },
      {
        "required": [
          "principal_id"
        ]
      },
      {
        "required": [
          "authn_method"
        ]
      }
    ]
  },
  "additionalProperties": false
}
